namespace IntegraRP.Domain.Routing;

public sealed record DistanceCache(Guid Id, Guid TenantId, string Status, DateTimeOffset CriadoEm)
{
    public Dictionary<string, object?> Metadata { get; init; } = new();
}
