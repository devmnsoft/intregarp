@model dynamic
<section class="ui-state ui-empty-state">
    <h2>@Model?.Title</h2>
    <p>@Model?.Description</p>
    <a class="ui-primary-action" href="@Model?.ActionUrl">@Model?.ActionText</a>
</section>
