@if (ViewData["HidePageHeader"] as bool? != true)
{
  <header class="page-header"><div><p class="eyebrow">Workspace premium</p><h1>@(ViewData["Title"] ?? "IntegraRP")</h1><p>@(ViewData["Description"] ?? "Operação integrada, governança e produtividade em tempo real.")</p></div></header>
}
