(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-tasks"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-tasks carregado.");
        }
    };
    window.integraFlow["flow-tasks"].init();
})();
