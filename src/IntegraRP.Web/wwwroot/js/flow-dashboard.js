(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-dashboard"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-dashboard carregado.");
        }
    };
    window.integraFlow["flow-dashboard"].init();
})();
