(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-api"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-api carregado.");
        }
    };
    window.integraFlow["flow-api"].init();
})();
