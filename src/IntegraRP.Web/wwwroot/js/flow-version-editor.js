(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-version-editor"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-version-editor carregado.");
        }
    };
    window.integraFlow["flow-version-editor"].init();
})();
