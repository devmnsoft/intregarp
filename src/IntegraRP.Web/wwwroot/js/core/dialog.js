(function(){"use strict";window.IntegraRPDialog={confirm:function(message,onConfirm){const el=document.getElementById("confirm-dialog");if(!el||!window.bootstrap)return;el.querySelector("#confirm-dialog-description").textContent=message;const modal=new bootstrap.Modal(el);const btn=el.querySelector("[data-confirm-dialog-primary]");btn.onclick=function(){modal.hide();if(onConfirm)onConfirm();};modal.show();}};})();
