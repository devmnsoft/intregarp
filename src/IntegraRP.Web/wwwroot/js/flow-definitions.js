(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-definitions"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-definitions carregado.");
        }
    };
    window.integraFlow["flow-definitions"].init();
})();
