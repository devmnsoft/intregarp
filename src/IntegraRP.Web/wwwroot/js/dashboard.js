(async function(){ const s=document.getElementById('dashboard-status'), cards=document.getElementById('dashboard-cards'); try{ const d=await fetch('/api/proxy?path='+encodeURIComponent('/api/dashboard')).then(r=>r.json()); s.textContent='Dashboard carregado com dados reais v1.9'; s.className='alert alert-success'; cards.innerHTML=Object.entries(d).map(([k,v])=>`<div class="col-md-3"><article class="card p-3"><small>${k}</small><strong>${v}</strong></article></div>`).join(''); }catch(e){ s.textContent='Erro ao carregar dashboard: '+e; s.className='alert alert-danger'; cards.innerHTML='<p>Estado vazio.</p>'; }})();
