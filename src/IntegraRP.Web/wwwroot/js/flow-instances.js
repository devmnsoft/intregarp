(function () {
    window.integraFlow = window.integraFlow || {};
    window.integraFlow["flow-instances"] = {
        init: function () {
            console.debug("Integra Flow módulo flow-instances carregado.");
        }
    };
    window.integraFlow["flow-instances"].init();
})();
