# v1.7 - Docker runtime report

Docker não está disponível no ambiente local desta execução. A validação esperada é executar `scripts/validate-docker-release.ps1` em host com Docker Desktop ou runner Docker habilitado.

Critérios: PostgreSQL, API, Web e Worker devem subir; API deve responder `/api/health`; Swagger deve abrir; endpoints de validação de release candidate e customer journey devem responder sem erro.
