# v1.7 - Smoke test report

Smoke tests reais dependem de API, Web, Worker, PostgreSQL e Docker em execução. No ambiente local atual essas dependências não estão disponíveis. Os scripts existentes em `scripts/smoke-test-api.ps1`, `scripts/smoke-test-web.ps1`, `scripts/smoke-test-worker.ps1` e `scripts/smoke-test-customer-journey.ps1` devem ser executados após `docker compose up -d` ou ambiente Windows equivalente.
