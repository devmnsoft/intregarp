# v1.33 — análise das falhas de workflow

## Base e método

- SHA base: `eb25e4660e49a92a9fbe50bb94355430d0f626cc`.
- O `git fetch origin --prune` foi executado antes das alterações, mas o proxy do ambiente respondeu HTTP 403.
- A CLI `gh` não está instalada neste ambiente. Por isso, os logs remotos não puderam ser baixados e nenhuma mensagem remota é apresentada como se tivesse sido lida.
- Estratégia escolhida: **A**. `ci.yml` concentra os gatilhos automáticos; os quatro workflows legados auditados ficam somente em `workflow_dispatch`.

## Runs solicitados

| Workflow | Run | Job/step/mensagem/arquivo/linha | Causa raiz observável | Correção/reexecução/conclusão |
|---|---:|---|---|---|
| indisponível localmente | 30276215648 | Não recuperado: `gh: command not found`; acesso HTTPS ao GitHub: `CONNECT tunnel failed, response 403` | Limitações do ambiente de execução, não uma inferência sobre a falha do produto | Pendente de reexecução no GitHub Actions; não homologado localmente |
| indisponível localmente | 30276215713 | Mesmo diagnóstico | Mesmo diagnóstico | Pendente de reexecução no GitHub Actions; não homologado localmente |
| indisponível localmente | 30276215570 | Mesmo diagnóstico | Mesmo diagnóstico | Pendente de reexecução no GitHub Actions; não homologado localmente |
| indisponível localmente | 30276214813 | Mesmo diagnóstico | Mesmo diagnóstico | Pendente de reexecução no GitHub Actions; não homologado localmente |

## Falhas locais comprovadas

| Check | Mensagem | Causa raiz | Situação |
|---|---|---|---|
| Portão Zero .NET | `/bin/bash: dotnet: command not found` | SDK .NET ausente na imagem | Não executado; deve rodar no CI |
| PostgreSQL 16 | `psql` ausente | Cliente e servidor PostgreSQL ausentes | Não executado; deve rodar no job `database-clean` |
| PowerShell | `pwsh` ausente | Runtime PowerShell ausente | Comparação Bash/PowerShell deve rodar no CI |

## Correções verificáveis no repositório

1. A 0034 passa a criar as chaves únicas antes das FKs, usa tipos canônicos `text` e cria cada FK em bloco independente.
2. A 0037 adiciona as colunas canônicas antes de índices e FKs.
3. A 0039 reconcilia instalações que receberam as versões históricas, cria FKs `NOT VALID` e as valida sem apagar órfãos.
4. O gerador usa `generatedAtUtc` ou `SOURCE_DATE_EPOCH`; Bash e PowerShell delegam ao mesmo gerador UTF-8/LF.
5. O CI compara byte a byte os resultados gerados e não permite que os workflows antigos concorram silenciosamente com a validação consolidada.

## Checksums históricos e corrigidos

| Migration | Publicado | Corrigido v1.33 |
|---|---|---|
| 0034 | `c9ec5ad960880d7ecc05e0c7c212ec270bc9511e87c7bf88a628b3169081f2e7` | `9d8ab0d6e7c3f7fb079e6fef9b618ad84b793d345985e0eb40796fa2a851b33c` |
| 0037 | `611999a1cb41ac8c70227dfa98e6ab44d00b0808cd6fbf7c65fc475a68f4545c` | `eb5f146ef9acc19d901d3ebd84ad31481f4f007c33af5a7591a9bd498d8e45ee` |
| 0039 | — | `975563742249dc8bbf086438299ca3874de13426a3a1a0448a65b441ff58b661` |

Nenhum workflow, PostgreSQL, jornada E2E ou runtime é declarado verde sem evidência de execução.
