# V1.8 — Build fix and functional start

## Checks executed

- `dotnet clean && dotnet restore && dotnet build && dotnet test` was attempted before feature work.
- The container does not have the .NET SDK available (`dotnet: command not found`), so compile/test execution is blocked by environment setup rather than source code at this point.

## Build fixes reviewed

- `src/IntegraRP.Application/Journey/JourneyUseCases.cs` is formatted with readable class blocks and no one-line journey class definitions.
- `src/IntegraRP.Worker/Program.cs` already imports `Microsoft.Extensions.Hosting` and `Microsoft.Extensions.DependencyInjection`.

## Functional start

This sprint starts the removal of fake CRUD behavior by adding real domain tables and PostgreSQL repository entry points for domain modules. The legacy `integrarp.v15_operational_object` table remains only as readiness/check metadata and must not be used as the storage table for product CRUDs.
