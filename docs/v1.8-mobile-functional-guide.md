# v1.8 — Guia funcional mobile

Escopo mobile: login, tenant, minha fila, O que fazer agora, detalhe da tarefa, checklist, evidência, conclusão, estado offline e sincronização simples.

Validação local: `npm install` e `npm run typecheck` executados com sucesso neste ambiente.
