# v1.17 — Matriz de maturidade funcional

| Área | Status | Evidência | Próximo passo |
|---|---|---|---|
| Auth API | parcial | Controller JWT persistido e migration de sessões/refresh. | Validar em PostgreSQL real no CI. |
| Auth Web | parcial | Arquitetura documentada; BFF existente preservado. | Finalizar telas MVC de conta. |
| Auth Mobile | parcial | Contrato legado deixa de simular token e aponta para auth real. | Adaptar app para chamar `/api/auth`. |
| Tarefas API | parcial | Endpoints e repositório PostgreSQL evoluídos. | Cobrir checklist obrigatório e anexos binários. |
| Fila offline | parcial | Estrutura SQL criada e endpoint queue existente preservado. | Implementar worker de conflitos. |
| Flow/Studio/BI | in-memory | Fora do escopo por regra do PR. | Incrementos dedicados. |
| Homologação executável | bloqueado | SDK .NET ausente no contêiner local. | Rodar CI com .NET 8 e PostgreSQL. |
