# v1.8 — Relatório de validação funcional

## Validação local obrigatória

- `dotnet clean`: não executado; SDK .NET indisponível no ambiente (`dotnet: command not found`).
- `dotnet restore`: não executado; SDK .NET indisponível.
- `dotnet build`: não executado; SDK .NET indisponível.
- `dotnet test`: não executado; SDK .NET indisponível.
- `npm install`: executado em `apps/IntegraRP.Mobile` com sucesso.
- `npm run typecheck`: executado em `apps/IntegraRP.Mobile` com sucesso.
- `docker compose build/up/ps`: não executado; Docker indisponível (`docker: command not found`).

## Correções registradas

- Criada migration 0020 com objetos idempotentes para auditoria de telas, catálogo funcional de templates e validação por módulo.
- Atualizado `database/scriptcompleto.sql` com os mesmos objetos da v1.8, mantendo schema `integrarp`.
- Adicionados testes estáticos para script completo, migration 0020 e documentação obrigatória.

## Próxima validação em CI

Executar GitHub Actions com SDK .NET, PostgreSQL e Docker para evidenciar restore/build/test, aplicação dupla do script completo e smoke E2E.
