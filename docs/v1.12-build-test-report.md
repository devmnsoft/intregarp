# v1.12 - Build e testes

Data: 2026-07-10.

## Execução local

| Comando | Resultado | Evidência |
|---|---|---|
| `dotnet clean` | Não executado | SDK .NET indisponível: `/bin/bash: dotnet: command not found`. |
| `dotnet restore` | Não executado | Bloqueado pela ausência do SDK .NET local. |
| `dotnet build` | Não executado | Bloqueado pela ausência do SDK .NET local. |
| `dotnet test` | Não executado | Bloqueado pela ausência do SDK .NET local. |
| `cd apps/IntegraRP.Mobile && npm install && npm run typecheck` | OK | `tsc --noEmit` concluído sem erros. |
| `pwsh -NoProfile -ExecutionPolicy Bypass -File scripts/db-debug-scriptcompleto.ps1` | Não executado | PowerShell indisponível: `pwsh: command not found`. |
| `scripts/db-debug-scriptcompleto.cmd` | Não executado | Ambiente Linux não executa `.cmd` diretamente: `Permission denied`. |

## Correções v1.12 relacionadas

- Criada API visual de homologação em `/api/homologation/status`, `/api/homologation/run-all` e `/api/homologation/run-check/{checkCode}`.
- Criado status de worker em `/api/validation/worker/status` com leitura real de `integrarp.outbox_evento` quando o PostgreSQL está disponível.
- Adicionados aliases de aceite para `/api/outbox`, `/api/billing/invoices/{invoiceId}/titles` e `/api/billing/titles/{titleId}/fake-boleto`.
- Criada tela `/homologation`, tela `/templates` e atalhos no menu lateral.

## Ação necessária no CI/ambiente homologação

Executar novamente em ambiente com SDK .NET e PostgreSQL:

```bash
dotnet clean
dotnet restore
dotnet build
dotnet test
```

Depois executar a validação do banco com PowerShell ou Windows CMD conforme `scripts/db-debug-scriptcompleto.ps1`/`.cmd`.
