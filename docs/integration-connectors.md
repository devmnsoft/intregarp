# integration-connectors

Conectores: credenciais armazenadas apenas como secret_reference, logs higienizados, correlation_id e status por execução.

## Como testar
Execute a migration 0014 ou o script completo em PostgreSQL sandbox. Use dados demo v1.2 e não configure credenciais reais.
