# v1.13 — Validação de build, testes e banco

## Resultado no ambiente Codex

Em 2026-07-13, a validação local foi iniciada a partir da raiz do repositório, mas o container não possui o SDK do .NET instalado.

| Comando | Resultado | Evidência |
| --- | --- | --- |
| `dotnet build --no-restore` | Não executado por limitação de ambiente | `/bin/bash: dotnet: command not found` |
| `dotnet clean` / `dotnet restore` / `dotnet build` / `dotnet test` | Bloqueado por limitação de ambiente | Todos retornaram `dotnet: command not found` / exit 127 |
| `cd apps/IntegraRP.Mobile && npm install && npm run typecheck` | Passou | `tsc --noEmit` finalizou com exit 0 |
| `scripts/db-debug-scriptcompleto.ps1` | Bloqueado por limitação de ambiente | `pwsh: command not found` / exit 127 |
| `scripts/db-debug-scriptcompleto.cmd` | Bloqueado por ambiente Linux sem execução CMD | `Permission denied` / exit 126 |
| `database/validation/validate_scriptcompleto_v19.sql` | Pendente | Requer PostgreSQL disponível |

## O que foi validado estaticamente

- Controllers operacionais auditados para remover `statusCode: 501` dos fluxos principais de clientes, produtos, estoque, pedidos e tarefas.
- SQL operacional novo permaneceu no repository PostgreSQL em Infrastructure, não em controllers.
- Endpoints operacionais alterados continuam protegidos por `[Authorize]`.
- As novas rotinas usam tenant e `excluido_em` para leitura/soft delete.

## Próxima validação obrigatória fora do Codex

Execute em ambiente com .NET SDK, PostgreSQL e PowerShell:

```bash
dotnet clean
dotnet restore
dotnet build
dotnet test
```

Depois valide o banco em uma base limpa e rode uma segunda vez para idempotência:

```powershell
scripts/db-debug-scriptcompleto.ps1
```

Também execute `database/validation/validate_scriptcompleto_v19.sql` e consulte `integrarp.vw_v19_demo_funcional_status` e `integrarp.vw_v19_o_que_fazer_agora`.
