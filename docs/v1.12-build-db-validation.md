# v1.12 — validação de build, testes e banco

Data da execução local: 2026-07-11.

## Comandos obrigatórios

| Comando | Resultado local | Observação |
|---|---|---|
| `dotnet clean` | Não executado | O SDK .NET não está instalado neste container (`dotnet: command not found`). |
| `dotnet restore` | Não executado | Bloqueado pela ausência do SDK .NET. |
| `dotnet build` | Não executado | Bloqueado pela ausência do SDK .NET. |
| `dotnet test` | Não executado | Bloqueado pela ausência do SDK .NET. |
| `pwsh -File scripts/db-debug-scriptcompleto.ps1` | Não executado | PowerShell não está instalado neste container (`pwsh: command not found`). |
| `scripts/db-debug-scriptcompleto.cmd` | Não executado | Script Windows sem permissão/executor nativo no Linux (`Permission denied`). |

## Correções aplicadas antes de evoluir

- O `V110RuntimeController` foi reduzido a uma camada obsoleta de compatibilidade, sem SQL inline, sem uso direto de Npgsql/Dapper e com `[Authorize]`.
- A lógica operacional emergencial foi direcionada para use cases em Application.
- O acesso PostgreSQL/Dapper/Npgsql do fluxo v1.10/v1.11 foi movido para repository em Infrastructure, com `tenant_id` parametrizado recebido do contexto da requisição.

## Validação de banco pendente no ambiente local

A validação completa do `database/scriptcompleto.sql` deve ser executada em ambiente com PostgreSQL e PowerShell/Windows Script disponível:

```powershell
pwsh -File scripts/db-debug-scriptcompleto.ps1
```

ou:

```cmd
scripts\db-debug-scriptcompleto.cmd
```

Critérios a confirmar no ambiente de CI/homologação:

- `database/scriptcompleto.sql` executa em banco limpo.
- `database/scriptcompleto.sql` executa uma segunda vez sem erro.
- `database/validation/validate_scriptcompleto_v19.sql` passa.
- `integrarp.vw_v19_demo_funcional_status` retorna checks.
- `integrarp.vw_v19_o_que_fazer_agora` retorna próxima ação.
- Não existem objetos indevidos em `public`.
- Não existem objetos indevidos em `integra`.
