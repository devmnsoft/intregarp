# Diagnóstico inicial v1.21

## Base

- Branch criada: `codex/v121-premium-workspace-ux-runtime-homologavel`.
- SHA base utilizado: `5ae25b012cfe7a7478c26aca0ac5d60fdf03747d`.
- `git fetch origin`: falhou por bloqueio de rede `CONNECT tunnel failed, response 403`; o checkout local já estava no commit base informado.
- `git pull --ff-only origin main`: falhou pelo mesmo bloqueio de rede; nenhum merge automático foi realizado.

## Portão técnico executado

| Comando | Resultado | Erro | Causa raiz | Correção/ação | Evidência |
| --- | --- | --- | --- | --- | --- |
| `dotnet --info` | Falha | `dotnet: command not found` | SDK .NET indisponível no ambiente | Registrado como limitação; CI v1.21 instala SDK | Terminal local |
| `dotnet sln IntegraRP.sln list` | Falha | `dotnet: command not found` | SDK .NET indisponível | Registrado como limitação | Terminal local |
| `dotnet clean/restore/build/test/format` | Falha | `dotnet: command not found` | SDK .NET indisponível | Não declarado como verde | Terminal local |
| `npm ci` | Passou com avisos | Depreciações de dependências transitivas | Pacotes legados do ecossistema React Native/Expo | Sem alteração funcional nesta versão | Terminal local |
| `npm run typecheck` | Passou | Nenhum erro TypeScript | N/A | N/A | Terminal local |
| `npm test --if-present` | Passou sem script configurado | N/A | Não há script de teste | Registrado como limitação | Terminal local |
| `psql ... script_completop.sql` | Falha ambiental | `psql not found` | Cliente PostgreSQL indisponível | CI usa PostgreSQL 16 e instala `postgresql-client` | Terminal local |
| `DatabaseInspector` | Falha ambiental | `dotnet not found` | SDK .NET indisponível | CI executa lint/manifest | Terminal local |

## Correções aplicadas

- Substituição do layout com CDN e menu inline por shell com partials, assets locais e navegação por serviço.
- Criação de autenticação visual premium via `AccountController`.
- Dashboard passou a usar ViewModel tipado e estados vazios honestos.
- CI atualizado para v1.21, `script_completop.sql`, PostgreSQL 16 e artefatos `v121`.
