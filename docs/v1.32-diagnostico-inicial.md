# Diagnóstico inicial v1.32

**Base:** `f8529a1bd86dde071ba03bf576a77b08d46f092f` (27/07/2026 UTC, Linux).

| Comando | Exit code | Resultado, causa e evidência |
|---|---:|---|
| `git fetch origin --prune` | 128 | Bloqueado pelo proxy (`CONNECT tunnel failed, 403`). O checkout local foi confirmado exatamente no commit-base exigido; não se declarou atualização remota. |
| `dotnet --info` | 127 | SDK .NET ausente (`dotnet: command not found`). Restore, build, format e testes .NET permanecem não homologados localmente. |
| `bash -n scripts/*.sh` | 0 | Todos os scripts Bash têm sintaxe válida. |
| `bash scripts/generate-script-completop.sh` | 0 | Artefatos v1.32 regenerados a partir das 38 migrations tipadas. |
| `cmp database/script_completop.sql database/scriptcompleto.sql` | 0 | Scripts completos idênticos byte a byte. |

## Causas raiz e correções

A 0037 já integra a versão publicada v1.31, portanto seu checksum foi preservado. A correção aditiva 0038 reconcilia colunas comerciais, integridade de totais, saldo, evidência multi-tenant e índices idempotentes. A numeração de pedidos baseada apenas em relógio foi substituída por sequência anual transacional protegida por advisory lock por tenant. O mascaramento textual de auditoria foi substituído por travessia JSON estruturada. A rota estável `/api/commercial` foi adicionada sem remover a compatibilidade v1.29, claims inválidas passaram a resultar em autenticação inválida e o worker fake de Mobile/AI deixou de ser iniciado.

## Limitações honestas

PostgreSQL 16, .NET, Windows, Playwright e GitHub Actions não puderam ser executados neste ambiente. O PR deve permanecer Draft até todos esses checks serem executados e ficarem verdes; nenhuma instalação, upgrade, preservação, E2E ou CI verde é alegada por este documento.
