# v1.15 — Diagnóstico inicial

> Ambiente local desta execução: branch `codex/v115-estabilidade-order-to-cash-purchasing`, base `31e8e06` (merge do PR #35). O container não possui `dotnet`, portanto a execução local de build/teste ficou bloqueada por ferramenta ausente; os workflows foram ajustados para publicar artefatos com `if: always()` para auditoria completa em CI.

## Falhas verificadas no repositório

| Workflow | Job | Step | Arquivo | Linha | Código | Mensagem completa | Causa raiz | Correção aplicada |
|---|---|---|---|---:|---|---|---|---|
| IntegraRP CI | build-test | Build/Test | `src/IntegraRP.Application/Runtime/OperationalRuntimeUseCases.cs` | 2, 73-83 | arquitetura | `ClaimsPrincipal`, `ClaimTypes` e `FindFirstValue` referenciados na camada Application | Dependência ASP.NET vazou para Application | Criado `ICurrentExecutionContext` na Application e adaptação HTTP na API via `ICurrentUserContext`. |
| IntegraRP CI | build-test | JWT config | `src/IntegraRP.Api/Extensions/ServiceCollectionExtensions.cs` | 20 | security | fallback de segredo JWT fixo de desenvolvimento | Segredo JWT versionado/fixo | Removido fallback; inicialização falha sem secret externo com mínimo de 32 caracteres. |
| Database Validation | database | migrations | `database/migrations/*.sql` | múltiplas | SQL | `INSERT INTO integrarp.schema_migrations(version)` | Migrations registravam histórico legado enquanto runner espera contrato novo | Removidos registros legados; runner passa a ser o único gravador de histórico. |
| Database Validation | database | migration runner | `src/IntegraRP.Infrastructure/Data/Migrations/PostgresMigrationRunner.cs` | 104-177 | SQL | conflito entre `version/aplicado_em` e `script_name/checksum_sha256/success` | Contratos divergentes entre `scriptcompleto.sql`, migrations e runner | `EnsureHistoryTableAsync` agora cria modelo v1.15, preserva tabela legada e migra `version` para `script_name` com checksum `legacy:`. |
| database-scriptcompleto-v19 | scriptcompleto | generate/validate | `database/scriptcompleto.sql` | 1 | process | script completo manual divergente das migrations | Arquivo era mantido manualmente e repetia inserts em `schema_migrations` | Criados geradores determinísticos `scripts/generate-scriptcompleto.sh` e `.ps1`; script regenerado a partir das migrations. |
| Pilot Validation | artifacts | evidence | `.github/workflows/*.yml` | múltiplas | CI | logs/trx/cobertura não publicados de modo uniforme | Evidências indisponíveis quando etapas falham | Jobs `publish-artifacts-v115` adicionados com `if: always()` e retenção de 30 dias. |

## Limitação de auditoria externa

Os logs completos do GitHub Actions do PR #35 não estão presentes no checkout local e não há remote configurado neste container. Por isso, este documento registra as falhas reproduzíveis por inspeção do estado pós-merge e deixa os workflows preparados para coletar evidências completas na próxima execução.

## Comandos executados localmente

```bash
dotnet clean IntegraRP.sln && dotnet restore IntegraRP.sln && dotnet build IntegraRP.sln --no-restore
# /bin/bash: line 1: dotnet: command not found

scripts/generate-scriptcompleto.sh
# geração concluída; validação PostgreSQL ignorada por ausência de psql/DATABASE_URL
```
