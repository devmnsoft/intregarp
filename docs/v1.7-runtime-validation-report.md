# v1.7 - Relatório de validação runtime

## Evidência local

- `dotnet clean`, `dotnet restore`, `dotnet build` e `dotnet test`: não executados localmente porque `dotnet` não está instalado no ambiente (`command not found`).
- `pwsh` e `psql`: indisponíveis no ambiente local.
- Docker: indisponível no ambiente local.
- Node: disponível; mobile deve continuar validado em CI com `npm install` e `npm run typecheck`.

## Correções aplicadas

- `database/scriptcompleto.sql` recebeu bloco v1.7 idempotente com `schema_migrations.description`, objetos críticos de smoke, índices, triggers, seeds demo e view `vw_v17_runtime_validation_status`.
- Criada migration documentada `0019_v17_runtime_validation_and_green_pipeline.sql`.
- Manifesto de migrations atualizado para v1.7.

## Limitação conhecida

A evidência final de PostgreSQL real, Docker real e .NET real deve ser coletada pelos workflows GitHub Actions e por execução local em host com SDK .NET 8, PostgreSQL, PowerShell e Docker.
