# IntegraRP v1.22 — Diagnóstico de build

- SHA base utilizado: `1bddde90549e062fd4514fe61eb89b93ab690122`
- Resultado do Portão Zero: bloqueado pelo ambiente; `dotnet` não está instalado (`command not found`).

## Comandos executados
- `dotnet --info` → falhou por ausência do SDK .NET.
- `dotnet sln IntegraRP.sln list` → falhou por ausência do SDK .NET.
- `dotnet clean IntegraRP.sln` → falhou por ausência do SDK .NET.
- `dotnet restore IntegraRP.sln` → falhou por ausência do SDK .NET.
- `dotnet build IntegraRP.sln --configuration Release --no-restore` → falhou por ausência do SDK .NET.
- `dotnet test IntegraRP.sln --configuration Release --no-build` → falhou por ausência do SDK .NET.
- `dotnet format IntegraRP.sln --verify-no-changes --no-restore` → falhou por ausência do SDK .NET.
- `dotnet build IntegraRP.sln --configuration Release --no-restore /p:TreatWarningsAsErrors=true` → falhou por ausência do SDK .NET.

## Correções aplicadas
- Removido uso de `FindUserAsync("", "")` no fluxo de troca de senha.
- `ChangePasswordUseCase` passou a localizar usuário por tenant/user, verificar senha atual, rejeitar reuso e revogar sessões preservando a atual.
- `RevokeSessionAsync` passou a exigir tenant, usuário e sessão.
