# IntegraRP v1.16 — Evidências de Teste

| Comando | Resultado | Observação |
| --- | --- | --- |
| `dotnet --info` | bloqueado | `/bin/bash: dotnet: command not found`. |
| `dotnet clean IntegraRP.sln` | bloqueado | SDK .NET ausente. |
| `dotnet restore IntegraRP.sln` | bloqueado | SDK .NET ausente. |
| `dotnet build IntegraRP.sln --no-restore` | bloqueado | SDK .NET ausente. |
| `dotnet test IntegraRP.sln --no-build` | bloqueado | SDK .NET ausente. |
| `cd apps/IntegraRP.Mobile && npm run typecheck` | funcional | TypeScript passou. |
| `cd apps/IntegraRP.Mobile && npm test` | bloqueado | Não há script `test`. |
| `cd apps/IntegraRP.Mobile && npm audit --omit=dev` | bloqueado | npm registry retornou 403. |
| `docker compose config` | bloqueado | Docker ausente. |
| `docker compose build` | bloqueado | Docker ausente. |
| `docker compose up -d` | bloqueado | Docker ausente. |
| `docker compose ps` | bloqueado | Docker ausente. |
