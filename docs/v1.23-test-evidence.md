# v1.23 - Evidência de testes

## Executado neste ambiente

- `bash -n scripts/*.sh`: passou com exit code 0.
- `node --check` nos JavaScripts próprios: passou com exit code 0.

- `cd apps/IntegraRP.Mobile && npm run typecheck && npm test --if-present`: passou com exit code 0; npm emitiu aviso não bloqueante sobre `http-proxy`.

## Bloqueado por ambiente

- Comandos `dotnet`: bloqueados por ausência do SDK .NET.
- PostgreSQL real/psql/pg_dump: não há serviço PostgreSQL 16 configurado no container.
