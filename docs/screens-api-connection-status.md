# Status de conexão das telas à API v1.3

As telas `/dashboard`, `/getting-started`, `/journey/what-to-do-now`, `/flow`, `/studio`, `/dynamic/{moduleCode}`, `/commercial/customers`, `/inventory/products`, `/orders`, `/billing`, `/connect/outbox`, `/bi`, `/project`, `/forms`, `/automation`, `/reports`, `/notifications`, `/operations` e `/ai/chat` devem consumir API real, tratar loading, erro, 401, 403 e estado vazio inteligente.

A view `integrarp.vw_v13_telas_com_dados_reais` lista os componentes já marcados como conectados.
